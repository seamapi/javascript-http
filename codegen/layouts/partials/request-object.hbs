{
{{#each parameters}}
  {{> doc}}
  {{json name}}{{#unless isRequired}}?{{/unless}}: {{> request-parameter-type }}{{#if isNullable}} | null{{/if}}{{#unless isRequired}} | undefined{{/unless}}
{{/each}}
}
